import type { MathTextStyle } from '@shared/lib/math-text';

/**
 * Single style selector used across static and dynamic math labels in Networks.
 */
export const NETWORKS_MATH_TEXT_STYLE: MathTextStyle = 'tex-like';
