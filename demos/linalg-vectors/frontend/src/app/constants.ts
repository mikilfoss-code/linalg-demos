import { type DatasetId } from '../lib/types';

export const VECTOR_WINDOW = 10;
export const DEFAULT_IMAGE_WIDTH = 28;
export const DEFAULT_IMAGE_HEIGHT = 28;
export const DEFAULT_DATASET: DatasetId = 'mnist';

